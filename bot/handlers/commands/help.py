def help_handler():
    return (
        "/start — Welcome message\n"
        "/help — List commands\n"
        "/health — Backend status\n"
        "/labs — List available labs\n"
        "/scores <lab> — Per-task pass rates"
    )
